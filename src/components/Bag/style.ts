"use client";

import styled from "@emotion/styled";

export const StyledBag = styled.div`
  z-index: 40;
  > img {
    position: fixed;
    bottom: 163px;
    right: 32px;
    cursor: pointer;
  }
`;
